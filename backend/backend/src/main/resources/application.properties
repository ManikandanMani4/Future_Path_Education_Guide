# ✅ Server (optional)
server.port=8080

# ✅ MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/future?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=mani

# ✅ JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ✅ Gemini API Key (replace with NEW key)
gemini.api.key=New_APi